<%= link_to cocktail_path(cocktail) do %>
  <div class="col-xs-12 col-md-4">
    <div class="card-category cocktail-card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path cocktail.photo, height: 300, width:400, crop: :fill %>')">
      <%= cocktail.name %>
    </div>
     <!--  <%= link_to cocktail_path(cocktail), method: :delete do %>
        <i class="fas fa-times cocktail-delete"></i>
      <% end %> -->
  </div>
<% end %>
